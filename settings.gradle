pluginManagement {
    repositories {
        mavenCentral()
    }
}

rootProject.name = 'loafer-boot-dependencies'

include "loafer-boot"

file("${rootDir}/loafer-boot-starters").eachDirMatch(~/loafer-.*-boot-starter/) {
    include "loafer-boot-starters:${it.name}"
}

